<div class="add-contact showgroup"
     id="contact_{{model.currentitem.ContactId}}">
    <div>
        <p>Имя: {{model.currentitem.FirstName}}</p>
        <p>Фамилия:  {{model.currentitem.LastName}}</p>
        <p>Отчество: {{model.currentitem.MiddleName}}</p>
        <p>Адрес: {{model.currentitem.Address}}</p>
        <p>Телефон: {{model.currentitem.Telephone1}}</p>
    </div>
    <!-- <div  ng-controller="membersgroupController">

             <button ng-click="showMembers(model.showgroup.GroupId)">Показать всех учатников</button>

     </div>-->
</div>

